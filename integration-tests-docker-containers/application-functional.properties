spring.datasource.url=jdbc:mysql://testdb:3306/prepay-accounts?useSSL=false&logger=com.mysql.cj.log.Slf4JLogger
# testdb is from the service name in the docker-compose file. 
# The port is the port inside the container, NOT the one exposed to the host.
